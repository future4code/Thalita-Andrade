import React from "react";

const AdminPage = props => {

    return (
        <div>
            <button>Criar novas viagem</button>
            <button>Todas as viagens</button>
            <button>Lista de Inscrições</button>
        </div>
    );
}

export default AdminPage;
import React from "react";

const HomePage = props => {

    return (
        <div>
            <button>Login</button>
            <button>Inscreva-se</button>
        </div>
    );
}

export default HomePage;